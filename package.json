{
  "name": "typingspeedometer",
  "displayName": "%displayName%",
  "publisher": "edvilme",
  "description": "%description%",
  "version": "1.2.0",
  "icon": "docs/img/icon.png",
  "engines": {
    "vscode": "^1.97.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/edvilme/typingspeedometer.git"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:type"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "typingspeedometer.showHighScore",
        "title": "%command.showHighScore.title%"
      },
      {
        "command": "typingspeedometer.textType",
        "title": "%command.textType.title%"
      },
      {
        "command": "typingspeedometer.shareStats",
        "title": "%command.shareStats.title%"
      }
    ],
    "keybindings": [
      {
        "command": "typingspeedometer.showHighScore",
        "key": "ctrl+alt+s",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "properties": {
        "typingspeedometer": {
          "type": "object",
          "description": "%config.typingspeedometer.description%",
          "properties": {
            "sessionTimeout": {
              "type": "number",
              "default": 3000,
              "description": "%config.sessionTimeout.description%",
              "minimum": 0,
              "maximum": 60000
            },
            "mediumSpeedThreshold": {
              "type": "number",
              "default": 50,
              "description": "%config.mediumSpeedThreshold.description%",
              "minimum": 0,
              "maximum": 500
            },
            "highSpeedThreshold": {
              "type": "number",
              "default": 80,
              "description": "%config.highSpeedThreshold.description%",
              "minimum": 0,
              "maximum": 500
            }
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.97.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.8",
    "eslint": "^9.32.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@vscode/l10n": "^0.0.18"
  }
}
